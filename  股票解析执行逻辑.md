针对股票类的解析逻辑处理（用于中国个人所得税：境外所得申报）

目标：从境外券商交易记录中，解析并产出“境外股息红利所得”和“境外财产转让所得（股票卖出）”的申报所需明细记录（重点是应纳税所得额/境外已纳税额等基础数据）。

不在本任务内实现的内容：
- 不计算中国应纳税额（税率/税额计算）
- 不计算境外税额抵免（抵免限额、实际抵免、可结转、最终应补税）

本文件约束/默认口径：
- 成本法：加权平均单价（移动加权平均；每次买入/企业行动后更新“持仓均价”，卖出按卖出前的均价结转成本）
- 手续费/税费：不计入成本（即不增加“财产原值”）。是否作为“合理税费”从转让收入中扣除，建议做成可配置（默认不扣除）。
- 汇总维度：按“来源国家（地区）”归集输出（为后续抵免/填报提供基础输入）。
- 亏损口径：直接输出负数（不在本任务内做“亏损抵扣/净额/归集规则”处理）。

---

## 1. 输入数据与标准化

### 1.1 交易记录最小字段集（建议）
- tradeDatetime：成交时间（含时区；至少要能落到日期）
- tradeDate：成交日期（若无可由 tradeDatetime 推导）
- symbol：股票/证券代码（最好同时带 isin/cusip 等唯一标识）
- side：BUY / SELL（至少要能区分买卖）
- quantity：成交数量（正数）
- price：成交均价（若缺失需补齐）
- currency：成交币种
- countryOrRegion：所得来源国家（地区）（没有就用“账户所在地/证券发行地”的映射规则补齐，并记录来源）
- fees：手续费（可选）
- taxes：交易相关税费（可选）
- dividendAmount：股息红利收入（仅股息类事件需要；可选）
- withholdingTax：境外预扣税（股息类常见；也可能在卖出时产生；可选）
- rawId/sourceRowId：原始行唯一标识（用于去重与可追溯）

### 1.2 标准化与清洗
- 去重：同一 rawId/sourceRowId 重复行；或用（账户+时间+方向+数量+价格+代码）等组合键去重
- 数值校验：quantity/price 不能为负；空值补齐（price、countryOrRegion、currency 等）
- 时间排序：按 tradeDatetime 升序；同一时间多笔需定义稳定排序（例如 BUY 先于 SELL，或按原始流水序号）
- 口径记录：任何“估算/补齐”的字段要打标（便于审计与重算）

---

## 2. 补齐证券信息与企业行动（Corporate Actions）

解析层需要能拿到并应用至少以下企业行动（按生效日排序应用到持仓台账）：
- 拆分/合股（split / reverse split）：(effectiveDate, ratio)
  - 调整规则（对持仓台账）：
    - positionQty = positionQty * ratio
    - avgUnitCost = avgUnitCost / ratio
    - positionCostTotal 保持不变
- 代码变更/合并换股等：建议通过证券主数据映射保持“同一标的”延续（否则会造成历史持仓衔接失败）

说明：本任务的核心是产出可申报的“财产原值/均价结转”明细；企业行动数据缺失会直接导致成本/原值错误，应作为高优先级依赖。

---

## 3. 持仓台账（加权平均单价 / 移动平均）

为每个证券维护一份“持仓台账”（不分批次），用来计算卖出时的“财产原值/卖出成本”。

建议维护的核心字段：
- positionQty：当前持仓数量
- positionCostTotal：当前持仓总成本（仅 buyPrice 累加；不包含 fees/taxes）
- avgUnitCost：加权平均单价 = positionCostTotal / positionQty（需要高精度保存；展示再四舍五入）
- currency / countryOrRegion：用于折算与归集（如跨国/跨币种需定义归属规则）

### 3.1 BUY 入账
- positionCostTotal += quantity * price
- positionQty += quantity
- avgUnitCost = positionCostTotal / positionQty
- fees/taxes：按当前口径“不计入成本”，不影响 avgUnitCost

### 3.2 SELL 出账（加权平均单价结转成本）
对一笔卖出 (sellQty, sellPrice)：
1) 取卖出前的均价 avgUnitCostBeforeSell = avgUnitCost
2) 计算本次卖出的“财产原值/卖出成本”：
   - originalValue = sellQty * avgUnitCostBeforeSell
3) 更新持仓台账：
   - positionCostTotal -= originalValue
   - positionQty -= sellQty
   - 若 positionQty > 0：avgUnitCost = positionCostTotal / positionQty；否则 positionCostTotal=0, avgUnitCost=0
4) 生成一条“财产转让所得明细”：
   - transferIncome = sellQty * sellPrice
   - reasonableTaxesFees = 0（默认；如启用“卖出费用可扣除”，则从 transferIncome 扣除，或单独输出供下游处理）
   - gainLoss = transferIncome - originalValue - reasonableTaxesFees

异常处理（建议）：
- 卖出数量 > 可用持仓：默认报错并进入异常表；如业务允许“期初持仓导入/推定成本”，必须显式配置并记录来源

### 3.3 企业行动对均价的影响（拆分/合股）
拆分/合股不会改变 positionCostTotal，只改变数量与单位成本表现。

对拆分比例 ratio（例如 4:1 则 ratio=4；1:10 合股则 ratio=0.1）：
- positionQty *= ratio
- avgUnitCost /= ratio
- positionCostTotal 保持不变

---

## 4. 税务相关记录产出（中国：境外所得申报，不算税额/抵免）

本任务建议把原始交易流水转换成“税务相关记录”，用于后续填报/计算：

### 4.1 境外股息红利所得记录（Dividend）
- 输入：dividendAmount（收入额）、withholdingTax（境外预扣税，可为 0）
- 人民币折算：
  - incomeCNY = fx(dividendCurrency, fxDate) * dividendAmount
  - foreignTaxPaidCNY = fx(dividendCurrency, fxDate) * withholdingTax（若 withholdingTax 与 income 币种不同，则分别折算）
- 产出字段（建议）：
  - taxableIncomeCNY：默认等于 incomeCNY（如后续有扣除项，应由下游模块处理）

### 4.2 境外财产转让所得记录（Capital Gain / 财产转让）
- 由 3.2 的“卖出明细”产生
- 对每条卖出明细：
  - income = transferIncome - reasonableTaxesFees
  - taxableBasis = originalValue（按卖出前的加权平均单价结转）
  - gainLoss = income - taxableBasis
  - taxableIncomeCNY = gainLossCNY（直接输出负数；下游再决定如何处理亏损/净额/抵扣）

---

## 5. 外币折算人民币（FX）

任何将用于申报/计算的金额（股息收入、转让收入、财产原值、境外已纳税额、合理税费等）都应在进入“税务汇总”前折算为人民币。

建议实现：
- fx(currency, fxDate) -> rate（并记录 rateSource 与 fxDate）
- fxDate 口径需配置并统一（例如交易日/结算日/其他口径），否则会导致同一交易在不同系统中金额不一致

---

## 6. 输出结构（建议与申报表字段对齐）

### 6.1 明细输出（可追溯）
- 股息红利明细：date、symbol、countryOrRegion、incomeForeign、incomeCNY、foreignTaxPaidCNY、taxableIncomeCNY
- 财产转让明细：sellDate、symbol、countryOrRegion、sellQty、sellPrice、avgUnitCostBeforeSell、transferIncomeCNY、originalValueCNY、reasonableTaxesFeesCNY、gainLossCNY、taxableIncomeCNY、foreignTaxPaidCNY(如有)、positionQtyAfterSell、positionCostTotalAfterSell、avgUnitCostAfterSell

### 6.2 汇总输出（用于填报/展示）
- 按国家（地区）+所得项目：收入、原值、合理税费、应纳税所得额、境外已纳税额

### 6.3 异常/风险输出
- 缺少买入导致均价/持仓台账不可信（例如期初持仓未导入）
- 无法获取企业行动导致成本错误风险
- 无法获取汇率导致折算失败
- 代码映射失败（同一证券代码变更）

---

## 7. 场景举例（含拆分）

说明：拆分只改变“股数/单价表现”，不改变持仓总成本；卖出时按“卖出前的加权平均单价”结转成本（财产原值）。

股票B（示例数据）

日期	描述	"持仓变动
（股）"	"实际买入单价
（元）"	"实际买入成本
（元）"	"持仓总额
（元）"	"持仓数
（股）"	加权平均单价（元）
2020年11月11日	买入	300	80	24000	24000	300	 80.00
2021年7月20日	4：1拆股	1200	20	0	24000	1200	 20.00
2024年5月1日	买入	200	30	6000	30000	1400	 21.43
2024年6月10日	10：1拆股	14000	7.5	0	30000	14000	 2.14
2024年12月1日	卖出	-13000	2.142857143	-27857.14286	2142.857143	1000	 2.14

						卖出成本/财产原值（加权平均单价）：13,000 股 × 2.142857143 元/股 = 27,857.14 元。

按本任务口径（加权平均单价）计算“财产原值/卖出成本”过程如下（高精度保存，展示可四舍五入）：
1) 2020-11-11 买入：positionQty=300，positionCostTotal=24,000，avgUnitCost=80
2) 2021-07-20 4:1 拆股后：
   - positionQty=300*4=1,200
   - positionCostTotal=24,000（不变）
   - avgUnitCost=80/4=20
3) 2024-05-01 买入 200 股@30：
   - positionCostTotal=24,000+6,000=30,000
   - positionQty=1,200+200=1,400
   - avgUnitCost=30,000/1,400=21.4285714286...
4) 2024-06-10 10:1 拆股后：
   - positionQty=1,400*10=14,000
   - positionCostTotal=30,000（不变）
   - avgUnitCost=21.4285714286.../10=2.14285714286...
5) 2024-12-01 卖出 13,000 股：
   - avgUnitCostBeforeSell=2.14285714286...
   - originalValue(卖出成本/财产原值)=13,000*2.14285714286...=27,857.142857...（展示为 27,857.14）
   - positionQtyAfterSell=14,000-13,000=1,000
   - positionCostTotalAfterSell=30,000-27,857.142857...=2,142.857143...（对应剩余 1,000 股）

备注：
- 若还需要“转让收入（卖出成交额）”，则为：13,000 股 × 卖出成交均价（未在该表中提供）。
- 本任务“手续费/税费不计入成本”，因此上述原值未包含任何费用；如后续需要把卖出费用作为“合理税费”扣除，应由下游模块处理或在本任务中单独输出费用字段。
